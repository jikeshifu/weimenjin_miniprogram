<form>

  <view class="cu-form-group">
    <view class="title">持有人</view>
    <input name="lockcard_username"  placeholder="" value="{{formData.pwd_name}}" bindinput="pwdNameSet"></input>
  </view>
  <view class="cu-form-group">
    <view class="title">密码</view>
    <input name="lockcard_remark"  placeholder="" value="{{formData.pwd}}" bindinput="pwdSet"></input>
  </view>
  <view class="cu-form-group">
    <view class="title">过期时间</view>
    <picker mode="multiSelector" value="{{dateIndex}}" bindchange="endDate" range="{{dateTimeArray}}" >
      <view class="picker">
        {{endtime}}
      </view>
    </picker>
  </view>

  <view class="padding flex flex-direction">
    <button class="cu-btn bg-green lg" bindtap="doSubmit">立即提交</button>
  </view>
</form>
